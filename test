if = { limit = { all_country = { if = {
                limit = {
                    is_in_faction_with = GER
                    is_major = yes
                }
                has_capitulated = yes
            }
        }
    }
    every_country = { limit = { OR = { is_in_faction_with = ENG is_in_faction_with = SOV } is_faction_leader = yes } every_country = { limit = { is_in_faction_with = GER is_faction_leader = yes } white_peace = PREV } } }